namespace HotOrCold.Dtos;

public record CreateDrinkDto (
    // Pas besoin pour l'instant
);

public record ReadDrinkDto (
    // Pas besoin pour l'instant
);

public record UpdateDrinkDto (
    // Pas besoin pour l'instant
);